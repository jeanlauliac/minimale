
export default component HelloWorld {
  props: {
    name: string,
  };

  render() {
    return
      <p>
        Hello, {this.props.name}!
      </p>;
  }

  // rename(name: string) {
  //   this.props.name = name;
  // }
}
