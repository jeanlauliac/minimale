
export default component HelloWorld {
  props: {
    name: string,
  };

  render() {
    return
      <p>
        Hello, <b>{this.props.name}</b>!
        <span>Foo.</span>
      </p>;
  }

  // rename(name: string) {
  //   this.props.name = name;
  // }
}
