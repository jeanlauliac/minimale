
export default component HelloWorld {
  state: {
    name: string,
  };

  render() {
    return
      <p>
        Hello, <b>{this.state.name}</b>!
        <br />
        <span>{'Foo'}.</span>
      </p>;
  }

  rename(name: string) {
    this.props.name = name;
  }
}
